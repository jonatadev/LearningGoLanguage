package main

import "fmt"

func dataTypes() {

	var approved bool = true
	var username string = "Jonatas" // String values must be surrounded by double quotes
	available := true

	var sum int = 100 // The type of integer to choose, depends on the value the variable has to store.
	var value float32 = 100.00

	fmt.Println(approved)
	fmt.Println(username)
	fmt.Println(available)

	fmt.Println(sum)
	fmt.Println(value)
}
